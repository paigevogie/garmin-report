{
  "name": "garmin-report",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "next": "^12.3.1",
    "react": "^18.2.0",
    "react-calendar": "^3.9.0",
    "react-dom": "^18.2.0"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "server": "python3 local.py",
    "pulldata": "eval $(grep '^API_KEY' .env) && curl -X POST https://garmin-report.vercel.app/api/index --header \"Authorization: Bearer ${API_KEY}\""
  }
}
